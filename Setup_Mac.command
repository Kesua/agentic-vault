#!/usr/bin/env bash
# Agentic Vault -- double-click this file in Finder to set up.
# If macOS says "permission denied", run once:  chmod +x Setup_Mac.command _setup/bootstrap_mac.sh
cd "$(dirname "$0")"
bash _setup/bootstrap_mac.sh
